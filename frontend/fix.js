const fs = require('fs'); const file = 'd:/Lena/KULIAH/Laragon/laragon/www/sistemsaas/frontend/src/api/mentorApi.js'; let data = fs.readFileSync(file, 'utf8'); data = data.replace(/previewCertificate: async \(idSubmission\) => \{[\s\S]*?\n  \}/, 'previewCertificate: async (idSubmission) => {\n    const response = await api.get(/mentor/interns//preview-certificate, { responseType: \'blob\' });\n    return URL.createObjectURL(response.data);\n  }'); fs.writeFileSync(file, data); console.log('Done');
